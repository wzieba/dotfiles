- clean: []
- link: 
    ~/.zshrc:
    ~/.hammerspoon:
    ~/.Brewfile:
    ~/.alacritty.yml:
- shell:
  - git submodule sync --recursive
  - 
      command: brew bundle --global --debug
      stdout: true

